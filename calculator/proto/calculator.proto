syntax = "proto3";

package calculator;

option go_package = "github.com/nathanfabio/sumAPI-gRPC/calculator/proto";

import "sum.proto";

service CalculatorService {
    rpc Sum(SumRequest) returns (SumResponse);
}